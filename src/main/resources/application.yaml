spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loan_manager
    username: postgres
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    defer-datasource-initialization: true

  sql:
      init:
          mode: always

